<h1>We got all the authors</h1>

<table>
  <thead>
    <th>Name</th>
    <th>Homepage</th>
  </thead>
  <tbody>
    <% @authors.each do |author| %>
      <tr>
        <td><%= author.name %></td>
        <td><%= link_to 'Visit', author.homepage %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New', new_author_path %>
